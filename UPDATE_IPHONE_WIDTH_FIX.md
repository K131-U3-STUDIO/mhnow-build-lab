# v0.6.1 iPhone幅修正1（UI2）更新手順

## 対象
この会話で配布した `MHNow_Build_Lab_v0.6.1_FULL_SOURCE_UI_FIXED.zip` が基準です。
火力計算、状態異常・属性倍率、検索アルゴリズム、所持漂移、保存形式は変更していません。
GitHub / 公開中のPagesへの変更は、この成果物作成時点では行っていません。

## 修正
スキル条件パネルが内容に押されて画面より広がる最小幅を修正しました。
iPhone幅は1列、カテゴリは折り返し。条件/Lvは画面内の2列とし、360px以下では縦積みです。
長いスキル名・選択条件・結果チップも折り返します。入力欄はスマートフォン幅で16px以上、選択欄44px以上にしました。
共有ヘッダーもスマートフォン幅で折り返すようにしました。
文書全体の overflow-x:hidden による切り捨てではありません。

## 入れ替える実行ファイル
リポジトリ直下の次の3つを同じコミットで更新してください。

- index.html
- release-manifest.json
- service-worker.js

index.htmlだけを先に公開しないでください。Service Workerはハッシュ照合するため、3つを同じ版に揃えます。
今回、js/・assets/・data/・icons/・manifest.webmanifestの実行データは変更していません。
以前のUI修正を未適用の場合や、別版を運用している場合は、この差分だけを適用せず基準版を照合してください。

## 同梱補助ファイル
- SHA256SUMS.txt：完全ソース版のファイルハッシュ（このファイル自体とキャッシュを除く）。
- UPDATE_IPHONE_WIDTH_FIX.md：この手順書。
- TEST_IPHONE_WIDTH_FIX.md：今回の検証結果と限界。
- tests/iphone_layout_check.py：画面幅の回帰検査。

これらも含め、パッチ内の階層のまま反映してください。既存のassetsは削除しません。
ルートにもう1つMHNow_...フォルダを作ってしまわないよう、ZIP内の配布フォルダの「中身」を反映します。
コミット名の例：Fix iPhone skill pane width v0.6.1 UI2

## 公開
既存のGitHub Actions公開方式を維持してください。
RC checksはpushだけでは自動実行しない設定です。必要な検査はActionsで手動実行、またはPRで確認します。
通常の `Generate, validate and publish MHN master` 公開ワークフローをmainで実行し、prepareとdeploy両方の成功を確認してください。
この既存ワークフローは外部masterの再生成も行います。手元の計算結果と同じmasterをそのまま検査する操作とは異なります。
公開ビルド中にengine/配信用assets/manifestが再生成されます。JSやmasterを後日変更するときも同じビルド手順で揃えてください。
Pagesをブランチ方式へ切り替える必要はありません。

## iPhoneでの確認
1. オンラインでSafariからサイトを開き、スキル指定へ移動します。
2. 横スクロールしなくてもスキル条件・条件/Lv・検索ボタンが収まることを確認します。
3. 見切りを必須Lv3、力任せを除外、別スキルを上限にして選択状態が保たれることを確認します。
4. ページ最下部の `UI: iPhone幅修正1` が更新確認用の表示です。
5. 旧画面のままなら「設定」→「キャッシュ更新」で新版取得・切替を確認し、再起動します。
6. 保存データやSafariのサイトデータを消す必要はありません。ホーム画面アイコンも作り直しません。

実機iPhone Safari / ホーム画面PWAのライフサイクル確認は未実施です。今回の検証は下記テスト報告の範囲です。
